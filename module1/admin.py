from django.contrib import admin
from .models import GameUser
# Register your models here.

admin.site.register(GameUser)
# This code registers the GameUser model with the Django admin site, allowing it to be managed through the admin interface.